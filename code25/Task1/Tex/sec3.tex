\section{实验任务} % [cite: 10]
\begin{enumerate}
    \item \textbf{汇编编程}：计算 $a = b \times c + d$。 % 
    \begin{itemize}
        \item $a$ 的值保存寄存器序号为：选课号对 7 取余的寄存器中。
        \item $b$ 的值为：学号中的班级号。
        \item $c$ 的值为：学号的后 2 位的值。
        \item $d$ 的值为：选课号。
    \end{itemize}
    
    \item \textbf{C程序调用汇编程序}： % [cite: 12]
    理解【例4.62】中 \texttt{strcopy} 函数的执行过程，将复制后的字符串变为自己的学号。
    
    \item \textbf{C语言中内嵌汇编程序}： % [cite: 13]
    理解【例4.65】的代码执行过程，将函数 \texttt{add(int i, int j)} 修改成 $i \times j + k$ 三个数运算的函数，并验证修改后的结果。
\end{enumerate}